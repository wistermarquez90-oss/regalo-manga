---
import Layout from '../layouts/Layout.astro';
import BotonTema from '../components/BotonTema.astro';

// Obtenemos la ruta base correcta para GitHub Pages
const base = import.meta.env.BASE_URL.endsWith('/') 
  ? import.meta.env.BASE_URL 
  : `${import.meta.env.BASE_URL}/`;

// Lista de las 7 canciones con sus IDs de YouTube y sus letras
const canciones = [
  {
    id: 'ac3HkriqdGQ',
    titulo: 'You and Me',
    artista: 'Lifehouse',
    letra: [
      "What day is it? And in what month?",
      "This clock never seemed so alive",
      "I can't keep up, and I can't back down",
      "I've been losing so much time",
      "'Cause it's you and me",
      "And all of the people with nothing to do, nothing to lose",
      "And it's you and me",
      "And all of the people, and I don't know why",
      "I can't keep my eyes off of you",
      "There's something about you now",
      "That I can't quite figure out..."
    ]
  },
  {
    id: 'NdKhS4MMCEo',
    titulo: 'Somos novios',
    artista: 'Luis Miguel',
    letra: [
      "Somos novios...",
      "Pues los dos sentimos mutuo amor profundo",
      "Y con eso ya ganamos lo más grande de este mundo.",
      "Nos amamos, nos besamos como novios",
      "Nos deseamos y hasta a veces sin motivo y sin razón, nos enojamos.",
      "Somos novios...",
      "Mantenemos un cariño limpio y puro",
      "Como todos, procuramos el momento más oscuro para hablarnos",
      "Para darnos el más dulce de los besos",
      "Recordar de qué color son los cerezos",
      "Sin hacer más comentarios, somos novios..."
    ]
  },
  {
    id: 'Zrn3I_LM9xA',
    titulo: 'Simplemente pasan',
    artista: 'Morat, Cami',
    letra: [
      "Hay cosas que simplemente pasan...",
      "Historias que se escriben sin planearlo,",
      "Momentos que se quedan grabados en el alma para siempre.",
      "Y cuando menos lo esperas, la vida te regala a la persona perfecta",
      "Para caminar de la mano, compartir risas y construir recuerdos inolvidables.",
      "Porque hay canciones y miradas que lo dicen todo sin necesidad de hablar.",
      "Simplemente pasan... y se vuelven lo mejor de nuestros días."
    ]
  },
  {
    id: 'HLeYvefzUFA',
    titulo: 'To love you more',
    artista: 'Céline Dion',
    letra: [
      "Take me back into the arms I love",
      "Need me like you did before",
      "Touch me once again, and remember when",
      "There was no one that you wanted more",
      "Don't go, you know you'll break my heart",
      "She won't love you like I will",
      "I'm the one who'll stay when she walks away",
      "And you know I'll be standing here still",
      "I'll be waiting for you here inside my heart",
      "I'm the one who wants to love you more..."
    ]
  },
  {
    id: '9Muiws78y5A',
    titulo: 'Eternamente bella',
    artista: 'Alejandra Guzmán',
    letra: [
      "Quisiera que me dijeras una y otra vez:",
      "\"Te quiero, baby, te quiero, siempre te querré\",",
      "Con esa lengua extranjera que me ablanda las piernas.",
      "Que tienes boca de azúcar, eso ya lo sé,",
      "Que besas con quemaduras de veneno y miel,",
      "Que me has cambiado, no hay duda, los otros también y me gusta.",
      "Por eso voy a hacer por ti, por ti, por ti",
      "Eternamente bella, bella...",
      "Con un hechizo de gitana seré la princesa encantada",
      "Que te amará por siempre..."
    ]
  },
  {
    id: 'JkK8g6FMEXE',
    titulo: "I don't wanna miss a thing",
    artista: 'Aerosmith',
    letra: [
      "I could stay awake just to hear you breathing",
      "Watch you smile while you are sleeping",
      "While you're far away and dreaming",
      "I could spend my life in this sweet surrender",
      "I could stay lost in this moment forever",
      "Every moment spent with you is a moment I treasure",
      "Don't want to close my eyes, I don't want to fall asleep",
      " 'Cause I'd miss you, babe, and I don't want to miss a thing",
      "'Cause even when I dream of you, the sweetest dream would never do",
      "I'd still miss you, babe, and I don't want to miss a thing..."
    ]
  },
  {
    id: '5mI4D2LHdCQ',
    titulo: 'Electricity',
    artista: 'Silk City, Dua Lipa',
    letra: [
      "Falling, I am falling",
      "Keep on calling, calling",
      "And oooh, I touch you once, I touch you twice",
      "I call your name twice, but it's no use",
      "You're electric, glowing in the dark",
      "And it feels like electricity between us",
      "Let it flow, let it shock the system",
      "We've got that special spark that never fades away."
    ]
  }
];
---

<Layout title="Nuestra Música Especial">
  
  <!-- Barra superior pegajosa -->
  <nav class="sticky top-0 w-full bg-white/70 dark:bg-[#0a0908]/80 backdrop-blur-md z-50 border-b border-violet-100 dark:border-white/10 animate-fade-in-down transition-colors duration-500">
    <div class="max-w-4xl mx-auto p-4 flex justify-between items-center">
      <a href={base} class="text-sm font-medium opacity-70 hover:opacity-100 hover:text-violet-700 dark:hover:text-amber-400 transition-all flex items-center gap-2">
        ← Volver al inicio
      </a>
      <h1 class="font-bold italic text-lg text-violet-950 dark:text-amber-100 tracking-wider">Rincón Musical</h1>
      <BotonTema />
    </div>
  </nav>

  <!-- Contenido Principal -->
  <main class="w-full max-w-4xl mx-auto p-6 relative z-10 flex flex-col gap-8 pb-20">
    
    <div class="text-center space-y-2 animate-fade-in-up">
      <h2 class="text-3xl md:text-5xl font-bold text-violet-900 dark:text-amber-400 tracking-tight">
        Nuestras Canciones
      </h2>
      <p class="text-base md:text-lg opacity-80 max-w-lg mx-auto">
        Elige una melodía, relájate y disfruta de su letra.
      </p>
    </div>

    <div class="grid grid-cols-1 md:grid-cols-12 gap-8 items-start">
      
      <!-- Lista de Canciones -->
      <div class="md:col-span-5 bg-white/40 dark:bg-black/30 backdrop-blur-md p-4 rounded-2xl border border-violet-100 dark:border-white/10 shadow-xl animate-fade-in-up flex flex-col gap-2">
        <h3 class="text-sm font-semibold uppercase tracking-wider px-3 py-2 text-violet-800 dark:text-amber-300">
          Lista de Reproducción
        </h3>
        <div class="flex flex-col gap-2 max-h-[450px] overflow-y-auto pr-1">
          {canciones.map((cancion, index) => (
            <button 
              type="button"
              data-index={index}
              class="song-btn text-left px-4 py-3 rounded-xl transition-all flex items-center justify-between group border border-transparent hover:border-violet-300 dark:hover:border-amber-500/30 hover:bg-violet-50/60 dark:hover:bg-white/10 cursor-pointer"
            >
              <div>
                <p class="font-bold text-gray-900 dark:text-gray-100 group-hover:text-violet-700 dark:group-hover:text-amber-400 transition-colors">
                  {cancion.titulo}
                </p>
                <p class="text-xs opacity-70">{cancion.artista}</p>
              </div>
              <span class="text-violet-600 dark:text-amber-400 opacity-0 group-hover:opacity-100 transition-opacity text-sm">▶</span>
            </button>
          ))}
        </div>
      </div>

      <!-- Reproductor y Letra -->
      <div class="md:col-span-7 bg-white/50 dark:bg-black/40 backdrop-blur-md p-6 md:p-8 rounded-3xl border border-violet-100 dark:border-white/10 shadow-2xl animate-fade-in-up flex flex-col gap-6">
        
        <div class="flex justify-between items-center border-b border-violet-100 dark:border-white/10 pb-4">
          <div>
            <span id="current-artist" class="text-xs font-semibold uppercase tracking-widest text-violet-600 dark:text-amber-400">Selecciona una canción</span>
            <h3 id="current-title" class="text-2xl font-bold text-violet-950 dark:text-white mt-1">Elige abajo</h3>
          </div>
          <button id="play-pause-btn" type="button" class="px-5 py-2.5 rounded-full bg-violet-600 hover:bg-violet-700 dark:bg-amber-500 dark:hover:bg-amber-600 text-white font-medium shadow-lg transition-all hover:scale-105 cursor-pointer text-sm">
            Reproducir / Pausa
          </button>
        </div>

        <div class="hidden">
          <div id="youtube-player"></div>
        </div>

        <div class="bg-white/60 dark:bg-black/40 p-6 rounded-2xl border border-violet-50 dark:border-white/5 shadow-inner min-h-[220px] max-h-[300px] overflow-y-auto flex flex-col justify-center text-center">
          <div id="lyrics-display" class="space-y-3 text-lg md:text-xl italic font-light opacity-90 leading-relaxed text-gray-800 dark:text-gray-200">
            <p class="opacity-60 not-italic text-sm">La letra aparecerá aquí al seleccionar una canción...</p>
          </div>
        </div>

      </div>

    </div>

  </main>

  <!-- Script Adaptado para Transiciones de Astro -->
  <script is:inline define:vars={{ canciones }}>
    let player = null;
    let currentSongIndex = 0;

    function initMusicPage() {
      // Si la API de YouTube ya está cargada, inicializamos el reproductor directamente
      if (window.YT && window.YT.Player) {
        createPlayer();
      } else {
        // Si no, cargamos el script de la API
        if (!document.getElementById('youtube-api-script')) {
          const tag = document.createElement('script');
          tag.id = 'youtube-api-script';
          tag.src = "https://www.youtube.com/iframe_api";
          const firstScriptTag = document.getElementsByTagName('script')[0];
          firstScriptTag.parentNode.insertBefore(tag, firstScriptTag);
        }
        window.onYouTubeIframeAPIReady = createPlayer;
      }
    }

    function createPlayer() {
      const playerDiv = document.getElementById('youtube-player');
      if (!playerDiv) return;

      // Limpiar contenedor previo si existe
      playerDiv.innerHTML = '';

      player = new YT.Player('youtube-player', {
        height: '0',
        width: '0',
        videoId: canciones[0].id,
        playerVars: {
          'autoplay': 0,
          'controls': 0,
        },
        events: {
          'onReady': () => {
            setupUI();
          },
          'onStateChange': onPlayerStateChange
        }
      });
    }

    function setupUI() {
      const buttons = document.querySelectorAll('.song-btn');
      const playPauseBtn = document.getElementById('play-pause-btn');

      buttons.forEach((btn) => {
        // Evitar duplicar listeners clonando el nodo o validando
        const newBtn = btn.cloneNode(true);
        btn.parentNode.replaceChild(newBtn, btn);
      });

      // Volver a asignar eventos a los botones limpios
      document.querySelectorAll('.song-btn').forEach((btn) => {
        btn.addEventListener('click', () => {
          const index = parseInt(btn.getAttribute('data-index'));
          loadSong(index, true);
        });
      });

      const newPlayBtn = playPauseBtn.cloneNode(true);
      playPauseBtn.parentNode.replaceChild(newPlayBtn, playPauseBtn);

      document.getElementById('play-pause-btn')?.addEventListener('click', () => {
        if (!player || typeof player.getPlayerState !== 'function') return;
        const state = player.getPlayerState();
        if (state === YT.PlayerState.PLAYING) {
          player.pauseVideo();
        } else {
          player.playVideo();
        }
      });

      // Cargar la primera canción visualmente sin reproducir de golpe
      loadSong(0, false);
    }

    function loadSong(index, autoPlay = true) {
      currentSongIndex = index;
      const song = canciones[index];

      document.getElementById('current-title').textContent = song.titulo;
      document.getElementById('current-artist').textContent = song.artista;

      const lyricsContainer = document.getElementById('lyrics-display');
      lyricsContainer.innerHTML = song.letra.map(line => `<p>${line}</p>`).join('');

      document.querySelectorAll('.song-btn').forEach((b, i) => {
        if (i === index) {
          b.classList.add('bg-violet-100', 'dark:bg-white/20', 'border-violet-300', 'dark:border-amber-500/40');
        } else {
          b.classList.remove('bg-violet-100', 'dark:bg-white/20', 'border-violet-300', 'dark:border-amber-500/40');
        }
      });

      if (player && typeof player.loadVideoById === 'function') {
        player.loadVideoById(song.id);
        if (!autoPlay) {
          player.pauseVideo();
        }
      }
    }

    function onPlayerStateChange(event) {
      const playPauseBtn = document.getElementById('play-pause-btn');
      if (event.data === YT.PlayerState.PLAYING) {
        if (playPauseBtn) playPauseBtn.textContent = 'Pausar ⏸';
      } else if (event.data === YT.PlayerState.PAUSED || event.data === YT.PlayerState.ENDED) {
        if (playPauseBtn) playPauseBtn.textContent = 'Reproducir ▶';
        
        if (event.data === YT.PlayerState.ENDED) {
          player.playVideo(); // Bucle automático al terminar la canción
        }
      }
    }

    // Se ejecuta al cargar inicialmente y en cada transición de página de Astro
    document.addEventListener('astro:page-load', initMusicPage);
  </script>
</Layout>